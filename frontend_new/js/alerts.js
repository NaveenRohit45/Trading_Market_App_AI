const c=document.getElementById('alerts');window.Alerts={update(a){c.innerHTML='';a.forEach(x=>c.innerHTML+=`<div class='alert'><b>${x.level}</b> - ${x.msg}</div>`);}};const d=[[{level:'INFO',msg:'Market opened bullish'},{level:'BUY',msg:'NIFTY breakout detected'}],[{level:'WARNING',msg:'Volatility increasing'},{level:'SELL',msg:'Exit long positions'}]];let i=0;Alerts.update(d[0]);setInterval(()=>{i=(i+1)%2;Alerts.update(d[i])},5000);